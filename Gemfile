source "https://rubygems.org"

# Keep the existing Pages engine versions; the Actions build loads site plugins directly.
gem "github-pages", "232", require: false

# Used by the shared image resolver and the build verification.
gem "nokogiri", "~> 1.18.0"
gem "minitest", "~> 5.25.0"
