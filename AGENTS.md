# VIO 网站协作约束

本文件是本仓库的最高优先级项目约束。开始任何工作前，必须同时阅读 `docs/PROJECT_BASELINE.md`。

## 开始修改之前

1. 读取远端 `main` 的最新提交和目标文件，不能使用旧对话、旧分支或本地缓存中的文件直接覆盖。
2. 检查最近提交，区分用户后来修改的内容与已有模板内容。
3. 明确本次允许修改的文件、组件或 CSS 选择器；未被点名的部分默认禁止改动。
4. 如果缺少准确的旧版本、参考图或原始资产，停止并说明缺少什么，不得凭记忆重新生成一个“类似版本”。

## 修改原则

- 只做用户要求的差异修改。修改一个问题时，不顺手调整其他布局、文案、颜色、间距、图片或交互。
- 不进行未被要求的重构、统一、润色、重新设计或“提升完成度”。
- 用户编写或手动修改过的内容属于受保护内容，未经明确要求不得改写。
- 恢复旧版本时，必须从 Git 历史提取原文件或原代码，禁止依靠描述重新生成。
- 已确认的图片、Logo 和视觉资产必须原样复用，禁止重新生成近似版本。
- 不新增虚构经历、城市故事、摄影叙事或文学化文案。
- 需要占位文字时，只使用“示例标题”“这是一段示例文字”“这是一段图片说明示例文字”等中性文字。
- 需要占位图片时，统一使用 `/assets/images/window-light.webp`，不得自行生成其他图片。
- 不删除暂时未使用的文件，除非用户明确要求删除。

## 当前页面逻辑

- 首页：保留大标题与整屏章节节奏；文章主卡片在左、装饰「文」在右，摄影集完整封面在右、图片轮廓图标在左。按日期倒序展示一个摄影主封面和两条次条，不复用摄影列表封面。
- 文章列表页：文章以卡片形式排列。
- 文章详情页：使用居中的正文版式。
- 摄影列表页：一个摄影集对应一张居中 OPTICS 刊头的杂志封面，桌面三列。
- 摄影详情页：沿用现有图集页面结构。

不得混淆文章列表、文章详情、摄影列表和摄影详情四种页面。

## 设计约束

- 所有品牌标志统一来自 `_includes/brand-mark.html` 和 `assets/images/favicon.svg`。
- 已确认的标题使用 Google Fonts `LXGW WenKai TC`，字重 `700`；具体范围以 `assets/css/title-font.css` 和 `docs/PROJECT_BASELINE.md` 为准。正文、导航和品牌继续使用现有无衬线字体，不得把标题字体扩大为全站字体。
- 基础配色为纸白 `#f7f3ee`、卡片白 `#fbf8f4`、墨色 `#25211f`、正文灰 `#4a4540`、辅助灰 `#8a8078`、珊瑚橙 `#c96749`。
- 文章详情正文样式以提交 `bc81a0f65ebb72372535cc9838851adc6420a2ec` 中的 `.article-content` 为历史基线（现名 `.journal-content`，整理时保留当前样式值）。
- 摄影列表继续使用居中 OPTICS 刊头、中文标题、细线、日期地点、底部 3:2 裁切照片的封面，保留三列和深浅配色。首页使用独立的完整摄影封面，与文章主卡片等宽等高，顶部裁切照片与底部标题、简介、年月日期和入口属于同一张卡片；不显示地点。历史母版与当前字段规则见 `docs/PROJECT_BASELINE.md`。不重新生成书册图片。
- 界面颜色以 `assets/css/main.scss` 的 `:root` 为唯一来源。标题、正文、辅助文字、强调色及背景按语义引用变量；透明色使用这些变量派生，不在摄影等独立样式中另写色号。照片与原始 Logo 不调色。

## 提交与检查

1. 每次写入前再次核对远端文件 SHA，避免覆盖用户刚提交的修改。
2. 一个任务使用一个清晰提交；提交信息只描述本次变更。
3. 修改后检查 GitHub Pages 构建状态。
4. 涉及视觉时至少检查桌面端和移动端；无法检查时必须如实说明。
5. 交付时列出：修改了什么、明确没有修改什么、提交号、已知遗留问题。

## 代码命名与写作入口（2026-09-09 整理）

- 文章统一使用 `_journal`、`site.journal`、`journal.html`、`journal-card` 和 `.journal-*`；摄影统一使用 `photography`。完整路径映射见 `docs/CODE_MAP.md`。
- 两类内容均支持 `YYYYMMDD_标题.md`，显示日期读取 `date`。不要再要求用户改成 Jekyll 内置文章的文件名形式。
- 图片放在 `assets/images/journal/` 与 `assets/images/photography/`，原始二进制内容不得改变；头像、图标、共用占位图保留共享位置。
- 构建先执行 `tools/content-build/cli.mjs prepare`，再运行支持 `_plugins` 的 Jekyll；不要恢复旧的安全模式构建步骤，否则文章图片和链接解析不会执行。
- 此次范围是代码与路径整理，不是重新设计。最新用户手动修改的样式值继续有效。

## 当前局部界面更新（2026-09-09）

- 文章列表为三列、较小圆角；文章详情抬头为底部分割线形式，不再恢复成大卡片。详见基线第 9 节。
- 顶栏滚动固定；两类详情的返回入口位于右下角阅读工具栏并指向各自列表。
- 首页摄影屏直接接页脚，不重新加入结束语占屏。
- 首页装饰为原 Noto Sans SC 900「文」字轮廓和粗笔画的图片图标，静态保留细描边，内部使用 `var(--card)` 浅色填充，不播放描边动画。位置与字号沿用用户当前值，见基线末节。Freeman 继续使用 OPTICS 子集，正文、标题字体与品牌字距保持不变。

- 关于页原头像保持 `/assets/images/about-avatar.svg`；鼠标进入圆形头像区域时随机显示 `assets/images/avatar/` 中的图片，离开时恢复原头像。候选图片在构建时自动发现，不手写文件清单，不修改用户上传的图片。

## 页面版式切换（2026-09-10）

- 两个列表页默认卡片，可切换细线列表；摄影列表缩略图使用纯图片，不加主题色外框。
- 文章详情默认原窄栏，可切换 50rem 宽栏；摄影详情默认 Full，可切换 Airy。切换只作用于当前页面，不从写作字段决定初始密度。
- 保留默认版式的原字体、颜色和卡片规则。新增状态集中在 `assets/css/layout-switches.css`；不得用独立提案的整页样式覆盖正式网站。旧摄影密度字段兼容但不输出局部覆盖。

## 固定阅读工具与篇末导航（2026-09-10）

- 详情页右下角横向工具栏依次为返回、分隔线、文章目录、成对版式按钮；摄影集没有目录。窄／宽使用页面内不同宽度的文字线条，Airy／Full 使用不同留白的图片图标，深色按钮表示已选中。顶栏和标题下不重复输出这些按钮。
- 目录从文章 h2/h3 自动生成，使用层级列表图标；目录位置下移，章节行紧凑排列，不显示“目录”标题或竖分隔线。展开不改变正文位置，1320px 以下使用可收起的浮层。
- 工具栏和目录受正文阅读区域底边约束，滚动到文末时停在页脚上方；摄影页预留工具栏停靠空间，不遮住篇末链接。
- 上一篇、下一篇放在文章和摄影集“完”字之后，分别左、右对齐，使用正文样式的箭头和真实标题；按各自列表的日期倒序衔接，缺少的一侧留空，不放进固定工具栏。
- 阅读交互集中在 `reading-tools` 的 include/CSS/JS 和 `reading-neighbors.html`。Airy 图文模块与普通图片共用版心宽度；页脚右侧只保留 RSS。用户源内容、主色板和两个列表页保持原样。
